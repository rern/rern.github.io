[Unit]
Description=Ready-to-use RTSP / RTMP / LL-HLS / WebRTC server and proxy
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/mediamtx /etc/mediamtx/mediamtx.yml

[Install]
WantedBy=multi-user.target
